---
profile: production

exclude_paths:
  - .ansible/
  - .venv/
  - .copier-answers.yml

# Several task names interpolate vars (e.g. the target Meilisearch version); the
# production profile's name[template] rule would otherwise flag them.
skip_list:
  - name[template]
